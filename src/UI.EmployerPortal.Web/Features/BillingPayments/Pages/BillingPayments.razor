@page "/billing-payments"
@using UI.EmployerPortal.Web.Features.BillingPayments.Components
@using UI.EmployerPortal.Web.Features.BillingPayments.Models

<PageTitle>Billing & Payments</PageTitle>
<div class="section-page">
    <Billing Source="@_navigationSource" />
</div>

@code {
    /// <summary>
    /// Populated from ?source=flow when entering from the payment workflow.
    /// Defaults to null (treated as Menu entry).
    /// </summary>
    [SupplyParameterFromQuery(Name = "source")]
    public string? Source { get; set; }

    private NavigationSource _navigationSource =>
        string.Equals(Source, "flow", StringComparison.OrdinalIgnoreCase)
            ? NavigationSource.Flow
            : NavigationSource.Menu;
}
